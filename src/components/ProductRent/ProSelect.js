import React from 'react'

function ProSelect({title,color}) {
    return (
        <div style={{color:color}} className='rentProduct_select'>
             {title}
        </div>
    )
}

export default ProSelect
